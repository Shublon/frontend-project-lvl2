name: run_eslint
on: [push]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: 
          npm ci
      - name: Run Linter
        run: 
          make lint