name: run_eslint
on: [push]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: 
          make install
      - name: Run Linter
        run: 
          make lint